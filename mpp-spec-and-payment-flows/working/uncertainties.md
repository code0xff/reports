# Uncertainties

- **Draft version** — the report cites `draft-ryan-httpauth-payment-01` [s01]; the IETF draft expires and is revised periodically, so field names may shift in later revisions.
- **session intent canonical fields** — sourced from secondary explainers [s08][s09] and Stellar's variant [s10] because the mpp.dev session page was 404 at fetch time. Treat field-level detail as reconstructed.
- **"sub-100ms / $0.0001" session figures** [s09] are vendor/explainer-stated and not independently benchmarked.
- **Payment method registry contents** — the enumerated method list (Tempo/Stripe/Lightning/Solana/Stellar/Monad/RedotPay/Card/Custom) is drawn from mpp.dev and Cloudflare docs [s03][s08]; the canonical IANA-style registry was not directly read.
- **Stripe `2026-03-04.preview` API version** [s07] is preview-stage and may change before GA.
