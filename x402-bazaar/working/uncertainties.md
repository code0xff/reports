# Uncertainties

- **Launch date (2025-09-10)** — from Yahoo Finance [s03]; the Coinbase launch page [s02] was access-limited so the date is corroborated via secondary coverage.
- **"200 ms" settlement** — vendor/coverage figure [s03][s10]; Yahoo itself notes real-world end-to-end is typically 300–500 ms once network latency is included.
- **"no facilitator fee for USDC on Base"** — stated by Coinbase per Yahoo [s03]; fee policy could change.
- **Quality-ranking signal list** — the CDP docs [s01] enumerate the signals (distinct buyers/30d, volume, recency, metadata completeness) but the exact blend is not published; treat ranking specifics as descriptive, not exact.
- **declareDiscoveryExtension API surface** — captured from the `bazaar.ts` example [s05]; field names may shift across x402 V2 releases.
