검토 결과 초안은 현재 수집한 1차 자료 범위 안에서만 주장하고 있으며, 공개 사양이 없는 구현 세부는 일반화하지 않았다.

- unsupported claims: 발견하지 못함
- citation integrity: 모든 핵심 주장에 `[^sNN]` 인용이 연결됨
- weak reasoning: `EAS vs DID`를 동일 계층 기술처럼 단정하지 않고 `EAS vs DID/VC stack`으로 구분함
- residual risk: DID method 선택, wallet UX, verifier 프로토콜(OpenID4VP 등)은 별도 조사 없이는 일반화하기 어렵다
